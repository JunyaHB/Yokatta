<% unless current_user.id == user.id %>
  <div id="follow_form">
  <% if current_user.following?(@users) %>
    <%= render 'unfollow' %>
  <% else %>
    <%= render 'follow' %>
  <% end %>
  </div>
<% end %>